# Bitcoin Core and Elements versions for integration tests.
# Busts CI daemon cache only when these change (not when pyproject.toml poe tasks change).
# Keep in sync with defaults in pyproject.toml (tool.poe.tasks.prepare-integration-daemons).
BITCOIN_REVISION=30.2
ELEMENTS_REVISION=23.3.2
